import datetime

class Exercise00
    STUDENT_NAME = "Khaled Houda"

    deadline()
        wantedDeadline = datetime.datetime(2023, 11, 15, 9, 0)
        return x.strftime("%B %d, %H:%M, %Y")
    deadline(string format)
        wantedDeadline = datetime.datetime(2023, 11, 15, 9, 0)
        return wantedDeadline.strftime(format)

